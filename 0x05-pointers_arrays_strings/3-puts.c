#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string followed by new line
 * @str: string-pointer parameter check
 *
 * Return: 0
*/

void _puts(char *str)
{
	puts(str);
	puts("\n");
}
