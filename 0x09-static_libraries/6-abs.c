#include "main.h"

/**
 * _abs - gives the absolute value of an int
 * @n: the int in question
 *
 * Return: absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
