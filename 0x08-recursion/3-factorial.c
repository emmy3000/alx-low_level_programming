#include "main.h"

/**
 * factorial - returns the factorial of an int
 *
 * @n: int param
 *
 * Return: int
 */

int factorial(int n)
{
	int i;

	if (n == 0)
		return (1);
	if (n < 0)
		return (-1);
	if
	{
		i = n * factorial(n - 1);
		return (i);
	}
}
